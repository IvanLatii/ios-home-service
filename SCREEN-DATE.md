# Екран `/date/[id]` — специфікація з Figma

> Джерело: вузол `402 / Date 1` (`2219:16390`). Значення точні, витягнуті з макета.
> Поточна реалізація не відповідає макету й підлягає переписуванню.

---

## Структура згори вниз

```
root                      bg neutral-50, flex-col
├── heading               sticky top-0, z-3, bg neutral-50
│   ├── Status bar        наявний StatusBarMock
│   └── toolbar           px-24 pb-16, gap-8, items-center
├── content
│   ├── h1 "Contact person"
│   ├── person
│   ├── h1 "Address"
│   ├── address
│   ├── h1 "Pick a date"
│   └── picker-container
│   ├── h1 "Pick time"
│   └── stack (слоти часу)
└── nav                   absolute bottom-0, z-1, градієнт-затухання
    ├── Continue
    └── Home Indicator
```

---

## Toolbar

Три елементи в ряд, `px-24 pb-16 gap-8`.

**Кнопка назад:** `40×40`, `bg neutral-0`, `p-12`, `rounded-6`, іконка `caret-left` `16×16`.

**Центр** — дві стрічки, вирівняні по центру, `flex-1`:
- `Contact, date & time` — Geist Medium 16/24, `neutral-800`
- `Step 2 of 4` — Geist Medium 12/12, `neutral-400`

**Кнопка закриття:** ідентична кнопці назад, іконка `x` `16×16`.

> Це той самий патерн тулбара, що вже реалізований на кроці 1. Переписувати не треба — перевикористати.

---

## Заголовки секцій

Усі три однакові: Alumni Sans SemiBold `36/32`, `neutral-800`, `tracking -0.36px`.

| Заголовок | Відступи |
|---|---|
| `Contact person` | `px-24 py-16` |
| `Address` | `px-24 pt-32 pb-16` |
| `Pick a date` | `px-24 pt-32 pb-16` |
| `Pick time` | `px-24 pt-32 pb-16` |

Перший має менший верхній відступ — це навмисно, він іде одразу під тулбаром.

---

## Рядки контакту й адреси

Однакова структура, `px-24`, `gap-12`, `items-center`.

**Ліворуч — іконка в контейнері:** `bg rgba(85,60,84,0.06)` (це `opacity-6%`, не `opacity-8`), `p-10`, `rounded-6`, іконка `20×20`.

**Праворуч — два рядки:**
- верхній: текст Geist Medium 16/24 `neutral-800` + іконка олівця `20×20` поруч, `gap-8`
- нижній: Geist Medium 12/12, `neutral-400`

| Блок | Іконка | Верхній рядок | Нижній рядок |
|---|---|---|---|
| person | `user` | `Ivan L` | `+380 68 006 54 88` |
| address | `map-pin` | `Shevchenka st., 47.` | `Rivne, Entrance 1, 5th Floor, Apartment 28` |

Олівці інертні — редагування в прототипі немає.

---

## Календар

Контейнер `px-8`. Картка: `bg neutral-0`, `rounded-10`, `pb-16`, `overflow-clip`.

**Хедер** `px-16 py-12`, `gap-12`:
- ліворуч `July 2026` — Geist **SemiBold** 16/24, `neutral-1000` + іконка `caret-right` `20×20`
- праворуч дві стрілки `caret-left` і `caret-right`, по `24×24`, `gap-12`

**Рядок днів тижня** `px-16 py-8`, `justify-between`, сім комірок по `44px`:
`MON TUE WED THU FRI SAT SUN` — Geist Medium 12/12, `neutral-400`, центровано.

**Сітка** — `flex-col gap-6`, кожен тиждень `px-16 justify-between`, комірка дня `44×44`.

Липень 2026 починається в **середу**, тому:
- перший тиждень: дві порожні комірки, далі `1 2 3 4 5`
- останній тиждень: `27 28 29 30 31`, далі дві порожні

**Стани дня:**
- звичайний — Geist Medium 16/24, `neutral-800`
- сьогодні (`15`) — Geist **SemiBold**, `blue-500`
- обраний (`17` у демо) — клікабельний

---

## Слоти часу

Контейнер `px-8 pb-16`, `flex-wrap`, `gap-4`.

Шість кнопок, кожна `flex-1`, `min-w-132`, `bg neutral-0`, `rounded-6`, `px-16 py-12`.
Текст — Geist Medium 16/24, `neutral-700`, центровано.

```
09:00 - 11:00   11:00 - 13:00
13:00 - 15:00   15:00 - 17:00
17:00 - 19:00   19:00 - 21:00
```

> **Увага:** у макеті шість слотів, у поточному коді п'ять. Бракує `19:00 - 21:00`.
> Роздільник — дефіс із пробілами, а не тире.

---

## Нижня навігація

`position: absolute`, `bottom-0`, ширина `402`, `z-1`.

Тло — градієнт затухання, щоб контент зникав під кнопкою:
```css
background-image: linear-gradient(to top, #f7f4f0, rgba(247, 244, 240, 0));
```

**Кнопка Continue:** `pt-8 px-24`, висота `56`, `px-18 py-16`, `rounded-8`, на всю ширину.
```css
background-image: linear-gradient(175.07deg, #0364ca 0%, #283694 47.617%);
```
Текст — Geist Medium 16/24, колір `blue-50` `#edf5ff`, центровано.

Під кнопкою — Home Indicator.

---

## Розбіжності з поточним кодом

- заголовка `Contact, date & time` немає
- порядок блоків інший: у макеті contact → address → date → time
- бракує секції адреси повністю
- п'ять слотів часу замість шести
- кнопка без брендового градієнта
- немає градієнта-затухання під навігацією
