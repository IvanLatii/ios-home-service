# Дизайн-токени

> Витягнуто з Figma-файлу `Home Service — iOS`, вузол `402 / Home`.
> Значення точні. Не вигадувати нові кольори й розміри — якщо потрібного токена немає, спитати, а не підбирати на око.

---

## Кольори

### Neutral — основа інтерфейсу

Тепла бежево-таупова шкала, не сіра. Це визначає характер продукту, тому підміняти на `gray-*` з Tailwind заборонено.

| Токен | HEX | Призначення |
|---|---|---|
| `neutral-0` | `#ffffff` | Білий, картки |
| `neutral-50` | `#f7f4f0` | Тло сторінки |
| `neutral-100` | `#efeae5` | Тло блоків, роздільники |
| `neutral-200` | `#ddd6cf` | Межі, неактивні стани |
| `neutral-300` | `#c8bfb8` | Межі активніші |
| `neutral-400` | `#afa39d` | Плейсхолдери, вимкнений текст |
| `neutral-500` | `#938783` | Проміжний вторинний |
| `neutral-600` | `#786c6e` | Вторинний текст |
| `neutral-700` | `#63515f` | Текст середньої ваги |
| `neutral-800` | `#553c54` | Основний текст |
| `neutral-1000` | `#261b26` | Найтемніший, контрастні поверхні |

### Pink — акцент

| Токен | HEX | Призначення |
|---|---|---|
| `pink-300` | `#f986b8` | Світлий акцент — ще не підтверджений у Figma |
| `pink-400` | `#ea5ea5` | Основний акцент |
| `pink-500` | `#d63e8a` | Підсвітка збігів у пошуку |
| `pink-600` | `#e95b99` | Ще не підтверджений у Figma |
| `pink-1000` | `#380c23` | Темний акцент, тло |

### Blue — вторинний

| Токен | HEX |
|---|---|
| `blue-50` | `#edf5ff` |
| `blue-100` | `#d8e9ff` |
| `blue-400` | `#5e97f0` |
| `blue-500` | `#0364ca` |
| `blue-800` | `#283694` |

### Yellow — попередження

| Токен | HEX | Призначення |
|---|---|---|
| `yellow-100` | `#fff2c7` | Тло попереджувального блоку |
| `yellow-700` | `#a87808` | Текст у ньому |

### Службові

| Токен | Значення |
|---|---|
| `opacity-8` | `#553c5414` — neutral-800 з альфою 8% |
| `opacity-20` | `#553c5433` — neutral-800 з альфою 20% |

---

## Типографіка

Два шрифти. Обидва безкоштовні.

**Geist** — увесь інтерфейсний текст. Є офіційний npm-пакет `geist`, зроблений Vercel, працює з `next/font` без налаштувань.

**Alumni Sans** — усі заголовки. Вузький високий гротеск, у Google Fonts.

| Токен | Шрифт | Розмір / інтерліньяж |
|---|---|---|
| `Headline/H1` | Alumni Sans SemiBold | 72 / 0.66 → ≈47.5px |
| `Headline/H3` | Alumni Sans SemiBold | 36 / 32 |
| `Headline/H4` | Alumni Sans SemiBold | 20 / 18 |
| `Body/16 Medium` | Geist Medium 500 | 16 / 24 |
| `Body/16 Regular` | Geist Regular 400 | 16 / 24 |
| `Body/14 Medium` | Geist Medium 500 | 14 / 20 |
| `Body/14 SemiBold` | Geist SemiBold 600 | 14 / 20 |
| `Label/14 Medium` | Geist Medium 500 | 14 / 14 |
| `Label/12 Medium` | Geist Medium 500 | 12 / 12 |

**Letter-spacing:** Figma повертає `-1` для всіх стилів без одиниці виміру. Найімовірніше це `-1%`, тобто `-0.01em`. Починати з цього значення й звіряти з макетом візуально.

---

## Радіуси

| Токен | Значення |
|---|---|
| `radius-4` | 4px |
| `radius-6` | 6px |
| `radius-8` | 8px |
| `radius-16` | 16px |
| `radius-full` | 999px |

---

## Тіні

**`shadow-inner`** — внутрішня, для втоплених елементів:
```css
box-shadow:
  inset 0 1px 2px 0 #1A15100D,
  inset 0 2px 4px 0 #1A151014;
```

**`shadow-xl`** — для модалок і плаваючих панелей:
```css
box-shadow:
  0 16px 32px -8px #1A151024,
  0 8px 16px -4px #1A15101A;
```

---

## Градієнти

**`Gradients/Brand`** — основна кнопка дії:
```css
background-image: linear-gradient(175.07deg, #0364ca 0%, #283694 47.617%);
```
Знайдений на кнопці `Continue` екрана `402 / Date 1`. Текст на ньому — `blue-50` `#edf5ff`.

**`Gradients/Grey+Ghost ↑`** — розмиття під нижньою навігацією, щоб контент зникав під кнопкою:
```css
background-image: linear-gradient(to top, #f7f4f0, rgba(247, 244, 240, 0));
```

---

## Чого бракує

**Два рожеві.** `pink-300` і `pink-600` є в коді, але ще не траплялись на опитаних вузлах. Підтвердити при роботі з відповідним екраном.

---

## Обережно: чужі токени

На екрані пошуку Figma віддає ще один набір — `SF Pro`, `Labels - Vibrant/Primary`, `Accents/Blue`, `Miscellaneous/Keyboards/*`. Це **не наша дизайн-система**, а системні токени Apple з UI-кіту, до якого належить компонент клавіатури.

У код їх не заводити. Клавіатура експортується картинкою (розділ 6.0 SPEC.md), тож ці значення не потрібні взагалі.

---

## Як витягувати те, чого бракує

Змінні повертаються **у розрізі вузла**: Figma віддає лише ті, що реально використані на конкретному екрані. Тому відсутність токена в цьому документі не означає, що його не існує — можливо, він застосований там, куди ще не заглядали.

Перед версткою кожного нового екрана має сенс опитати його вузол окремо й доповнити цей документ.

---

## Правило

Будь-яке значення кольору, шрифту, радіуса чи тіні в коді має посилатись на токен звідси. Якщо в макеті трапилось щось, чого тут немає — це сигнал, що токен не витягнутий, а не дозвіл підібрати схоже.
